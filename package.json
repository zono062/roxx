{
  "name": "roxx",
  "private": true,
  "version": "1.0.0",
  "description": "ROXX — HYROX完走プログラム（Web版＋iOSアプリ）",
  "scripts": {
    "build:www": "sh scripts/build-www.sh",
    "ios:sync": "npm run build:www && npx cap sync ios",
    "ios:open": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^8.5.2",
    "@capacitor/ios": "^8.5.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.2"
  }
}
